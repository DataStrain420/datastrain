# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Node
node_modules/
.next/

# Environment
.env
.env.local
.env.*.local

# Database (SQLite dev files only — Postgres in prod)
*.db
*.db-journal
*.db-wal
*.db-shm

# Uploads — seeded images are committed so the preview deploy isn't blank.
# Real user uploads in prod go to GCS (see GCS_BUCKET_NAME).

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db
